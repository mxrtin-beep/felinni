pandas>=2.0
numpy>=1.24
matplotlib>=3.7
flask>=3.0
# Optional, only needed for felinni.geocode / mapping:
geopy>=2.4
folium>=0.15
# requests is used by felinni.calendar_sources (importing Google/Outlook/Apple
# ICS calendars) and felinni.future_events (the Future tab's DuckDuckGo search):
requests>=2.31
# Optional, only needed for felinni.calendar_sources:
icalendar>=5.0
